import { PrimitiveGallery } from './pages/PrimitiveGallery';

/**
 * Phase 1 milestone — render the PrimitiveGallery for visual review.
 * Phase 2 will replace this with the AppShell (top bar, rail, dock zones).
 */
export const App = () => {
  return <PrimitiveGallery />;
};
